{
  "name": "@workshop/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --env-file-if-exists=../../.env --watch src/server.ts",
    "start": "node --env-file-if-exists=.env --env-file-if-exists=../../.env src/server.ts",
    "test": "node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workshop/engine": "workspace:*",
    "@workshop/shared": "workspace:*",
    "fastify": "^5.8.5",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "@types/pg": "^8.11.10",
    "typescript": "^6.0.3",
    "zod": "^4.4.3"
  }
}
