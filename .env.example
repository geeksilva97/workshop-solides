# Copy to .env (gitignored) before running `pnpm dev`.
# Matches the docker-compose Postgres service.
DATABASE_URL=postgres://solides:solides@localhost:5432/solides

# Optional: override the Ollama host (defaults to http://localhost:11434).
# OLLAMA_HOST=http://localhost:11434
